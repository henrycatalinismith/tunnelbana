language: node_js
node_js:
  - "7"
before_script:
  - npm run build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  repo: tunnelbana.github.io
  target_branch: master
  on:
    branch: master
